// This component was used by the old anon/guest flow. It's no longer
// referenced anywhere — usernames are collected at sign-up instead.
// Keeping this file as a stub because the sandbox filesystem doesn't
// let us delete it; safe to remove manually from the repo.

export function UsernameClaim() {
  return null;
}
